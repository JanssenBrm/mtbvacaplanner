import { OrderByDistancePipe } from './order-by-distance.pipe';

describe('OrderByDistancePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderByDistancePipe();
    expect(pipe).toBeTruthy();
  });
});
